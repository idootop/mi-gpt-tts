{
  "name": "mi-gpt-tts",
  "version": "1.3.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "npm run build && npm run start",
    "start": "node --env-file=.env server.js"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "@types/ws": "^8.5.10",
    "tsup": "^8.1.0",
    "tsx": "^4.15.5",
    "typescript": "^5"
  }
}
